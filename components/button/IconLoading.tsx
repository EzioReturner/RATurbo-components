import React from 'react';
import Loader from 'react-feather/dist/icons/loader';

const LoadingIcon: React.FC = () => (
  <span className="tc-icon-loading-circle">
    <Loader />
  </span>
);

export default LoadingIcon;
